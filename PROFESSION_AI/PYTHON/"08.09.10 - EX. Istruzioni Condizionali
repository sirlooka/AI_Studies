# -*- coding: utf-8 -*-
"""08.09.10 - EX. Istruzioni Condizionali

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1C3KTnqsCKGJ13pDsXpbewABWD736kj_Z

In questa esercitazione dovrai realizzare un programma di gestione avanzata di accesso e controllo dati utilizzando esclusivamente le istruzioni condizionali che hai già studiato (if, else, elif), gli operatori di confronto e logici. Il programma riceverà in input una serie di dati relativi a un ipotetico sistema di autenticazione e a regole di validazione per un utente.
Il tuo compito è implementare una logica complessa che verifichi diverse condizioni in cascata, fornendo output precisi e dettagliati, a seconda del risultato dei controlli.

<br><br><br>
Il programma deve seguire questi passi:

1. Ricevere in input tre stringhe: una email, una password e un ruolo (ad esempio: "admin", "user", "guest").
2. Verificare se l'email inserita corrisponde esattamente all'email "esempio@dominio.com".
   - Se l'email non è corretta, stampare "Accesso negato: email errata."
3. Se l'email è corretta, verificare la password confrontandola con la password "S3cur3P@ss".
   - Se la password non è corretta, stampare "Accesso negato: password errata."
4. Se email e password sono corrette, valutare il ruolo:
   - Se il ruolo è "admin", stampare "Accesso consentito con privilegi di amministratore."
   - Se il ruolo è "user", stampare "Accesso consentito con permessi standard."
   - Se il ruolo è "guest", stampare "Accesso consentito con permessi limitati."
   - Per qualsiasi altro valore del ruolo, stampare "Ruolo non riconosciuto: accesso limitato."

5. Infine, dopo il messaggio di accesso, esegui un controllo aggiuntivo sulla lunghezza della password:
   - Se la password è inferiore a 8 caratteri, stampare "Attenzione: password troppo corta."
   - Se la password è uguale o superiore a 8 caratteri, non stampare nulla.

Il programma deve gestire correttamente tutti i casi possibili, eseguendo i controlli in sequenza e utilizzando annidamenti e concatenazioni di condizioni a più livelli con if, elif ed else.

Ricordati di usare l'indentazione corretta per definire i blocchi condizionali, e attenzione alla valutazione delle espressioni booleane e agli operatori logici AND, OR e NOT quando necessario.

Come input, supponi che le stringhe vengano inserite una per riga tramite input().

Come output, stampa esattamente i messaggi indicati, uno per riga, senza altre righe o spazi.

Questo esercizio testerà la tua padronanza delle istruzioni condizionali avanzate e della capacità di strutturare un controllo del flusso dettagliato e preciso in Python.
Esempi Input/Output
input:
esempio@dominio.com
S3cur3P@ss
admin
output:
Accesso consentito con privilegi di amministratore.

<br><br><br>
input:
esempio@dominio.com
12345
user
output:
Accesso negato: password errata.

input:
wrong@dominio.com
S3cur3P@ss
guest
output:
Accesso negato: email errata.

input:
esempio@dominio.com
S3cur3P@ss
guard
output:
Ruolo non riconosciuto: accesso limitato.

input:
esempio@dominio.com
pass
user
output:
Accesso negato: password errata.
Attenzione: password troppo corta.

input:
esempio@dominio.com
S3cur3P@ss
guest
output:
# Accesso consentito con permessi limitati.
"""

#Ricevere in input tre stringhe: una email, una password e un ruolo (ad esempio: "admin", "user", "guest").

mail = input("Scrivi la tua email: ")
psw = input("Scrivi la tua password: ")
role = input("Scrivi il tuo ruolo: ")

# Verificare se l'email inserita corrisponde esattamente all'email "esempio@dominio.com".

if mail != "esempio@dominio.com":
  print("Accesso negato: email errata") # Se l'email non è corretta, stampare "Accesso negato: email errata."


# Se l'email è corretta, verificare la password confrontandola con la password "S3cur3P@ss".

elif psw != "S3cur3P@ss":
  print("Accesso negato: password errata") # Se la password non è corretta, stampare "Accesso negato: password errata."


# Se email e password sono corrette, valutare il ruolo:

else:
  if role == "admin":
    print("Accesso consentito con privilegi di amministratore.") # Se il ruolo è "admin", stampare "Accesso consentito con privilegi di amministratore."

  elif role == "user":
    print("Accesso consentito con permessi standard.") # Se il ruolo è "user", stampare "Accesso consentito con permessi standard."

  elif role == "guest":
    print("Accesso consentito con permessi limitati.") # Se il ruolo è "guest", stampare "Accesso consentito con permessi limitati."

  elif role != "admin" or role != "user" or role != "guest":
    print("Ruolo non riconosciuto: accesso limitato.") # Per qualsiasi altro valore del ruolo, stampare "Ruolo non riconosciuto: accesso limitato."

